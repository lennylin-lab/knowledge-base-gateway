# Frontend Quality Guidelines

The repository has no frontend source, lint configuration, browser tests or accessibility surface. Frontend quality checks are not applicable to the gateway MVP. A future UI must add its own toolchain and tests in the same task and must not weaken backend contract, security or redaction tests.

